export enum RoutesPath {
  HOME = "/user",
  LOGIN = "/login",
  SETTINGS = "/settings",
  PRODUCT = "/product/:id",
  CREATEPRODUCT = "/create-product",
  USER = "/",
  REGISTER = "/register",
  UPDATEUSER = "/user/:id",
}
